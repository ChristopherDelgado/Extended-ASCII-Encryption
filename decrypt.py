# should decrypt the given message using the given key and return the unencrypted message
# has a function called a decrypt takes in a encrypted message and a key
# using the key we decrypt the message and return the unencrypted in a txt file in the same directory as decrypt.py
